;
; HTTP/WebSocket Configuration for ARI
; /etc/asterisk/http.conf
;

[general]
enabled=yes
bindaddr=0.0.0.0
bindport=8088
tlsenable=no

; Enable built-in HTTP server
enablestatic=yes

; ARI WebSocket Support
; The URI for the ARI WebSocket is ws://localhost:8088/ari/events
